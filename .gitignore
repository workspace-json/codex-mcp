node_modules/
dist/
*.log
.DS_Store
# Built VS Code extension artifact; produced by `npm run build:extension`, shipped in the npm tarball, not source.
vsix/
*.vsix
# Keep local narration sources, voices, and generated production assets out of this public repository.
demo/

# Local solo-work coordination notes; never part of the product artifact.
.local/

# Local browser-automation state and one-off capture; never product assets.
.playwright-mcp/
hac120-desktop.png

# Local Claude state is machine-specific, not product source.
.claude/

# Local Codex transcript-processing scratchwork may contain prompts and tool traces.
/scripts/extract-session*.mjs

# Private Codex workflow guidance stays available in this checkout, not the OSS repo.
/AGENTS.local.md
/.agents/skills/
