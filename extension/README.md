# workspace.json Codex Decorations (spike)

Build a local VSIX with `npm ci && npm run package`. Install that generated file with `code --install-extension <path>.vsix` or `cursor --install-extension <path>.vsix`. This is a local spike artifact, not a published release.
